# Configuration Example
# Copy this file to config.yaml and modify with your own information

# GitHub username (for fetching public repository info)
github_username: "octocat"

# Optional GitHub token for local runs.
# Public user/repository/README data works without a token, but the official
# contribution graph uses GitHub GraphQL and needs authentication.
# GitHub Actions automatically provides GITHUB_TOKEN; no personal token is
# needed for the bundled deployment workflow.
# github_token: "ghp_xxxxxxxxxxxx"

# Server port
port: 8004

# RSS feed subscriptions (can have multiple)
rss_feeds:
  - url: "https://example.com/blog/feed.xml"
    name: "My Blog"

# Personal bio
bio:
  name: "Your Name"
  title: "Developer & Creator"
  description: "A passionate developer building cool things."
  avatar: "/static/avatar.png"

# Social links
social:
  github: "your-github-username"
  email: "your@email.com"
  twitter: "your-twitter"

# Footer text (optional)
footer:
  text: "Created by Your Name"
